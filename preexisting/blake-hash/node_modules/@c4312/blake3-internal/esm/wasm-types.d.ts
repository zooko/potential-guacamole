export interface WasmModule {
    HEAPU8: Uint8Array;
    _free(addr: number): void;
    _malloc(bytes: number): number;
    ccall<T extends number | boolean>(...args: unknown[]): T;
}
export declare const setWasm: (m: WasmModule) => WasmModule;
export declare const getWasm: () => WasmModule;
export declare const MAX_SCRATCH_SIZE: number;
declare class Scratch {
    private addr?;
    private scratchSize;
    get size(): number;
    grow(n: number): number;
}
export declare const scratch: Scratch;
export declare class HashRaw {
    private hasher;
    static default(): HashRaw;
    static keyed(key: Uint8Array | Uint8ClampedArray): HashRaw;
    static derive(key: Uint8Array): HashRaw;
    private addr?;
    readonly scratch: Scratch;
    constructor(hasher: number);
    update(address: number, length: number): void;
    read(seek: bigint, address: number, length: number): void;
    clone(): HashRaw;
    dispose(): void;
}
export {};
